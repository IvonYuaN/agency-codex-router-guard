interface:
  display_name: "Agency Codex Router Guard"
  short_description: "Route the right squad, persist project memory, and prevent spinning"
  default_prompt: "Read this workspace, choose the best specialist squad for the current project, persist it in .codex/project-profile.md, apply guardrails and escalation rules when needed, and then execute the task using that routing."
